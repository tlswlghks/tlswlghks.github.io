  <nav class="nav__list">
    <input id="ac-toc" name="accordion-toc" type="checkbox" />
    <label for="ac-toc">카테고리</label>
    <ul class="nav__items" id="category_tag_menu">
        <li>
            📂 카테고리
        </li>
        <li>
            <span class="nav__sub-title" style="margin: 0; margin-left: 10px; cursor: pointer;" onclick="toggle('PaperReview')">PaperReview</span>
            <ul id="blogSubMenu" style="display: none;">
                {% for category in site.categories %}
                    {% if category[0] == "FL" %}
                        <li><a href="/FL/" class="" style="margin-left: 15px;">- Blog ({{ category[1] | size }})</a></li>
                    {% endif %}
                    {% if category[0] == "MU" %}
                        <li><a href="/MU_1/" class="" style="margin-left: 15px;">- Blog ({{ category[1] | size }})</a></li>
                    {% endif %}
                    {% if category[0] == "AI" %}
                        <li><a href="/GAI/" class="" style="margin-left: 15px;">- Blog ({{ category[1] | size }})</a></li>
                    {% endif %}
                {% endfor %}
            </ul>
        </li>
        <li>
            <span class="nav__sub-title" style="margin: 0; margin-left: 10px; cursor: pointer;" onclick="TechnicalReport()">TechnicalReport</span>
        </li>
        <li>
            <span class="nav__sub-title" style="margin: 0; margin-left: 10px; cursor: pointer;" onclick="DL/ML()">DL/ML</span>
        </li>
    </ul>

    <script>
        function toggle(id) {
            var subMenu = document.getElementById(id);
            if (subMenu.style.display === "none") {
                subMenu.style.display = "block";
            } else {
                subMenu.style.display = "none";
            }
        }

        function TechnicalReport() {
            window.location.href = "/TR/";
        }
        function DL/ML() {
            window.location.href = "/DLML/";
        }
    </script>
    </ul>
  </nav>